#include <bits/stdc++.h>

#define PII pair<int,int>
using namespace std;
typedef long long ll;

int N;

int main()
{
    ios::sync_with_stdio(0), cin.tie(0);

    cin >> N;
    N % 2 ? cout << "SK" : cout << "CY";
    
    return 0;
}